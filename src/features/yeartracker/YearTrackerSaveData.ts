export class YearTrackerSaveData {
    month: number;
    monthProgress: number;


    constructor(month: number, monthProgress: number) {
        this.month = month;
        this.monthProgress = monthProgress;
    }
}
